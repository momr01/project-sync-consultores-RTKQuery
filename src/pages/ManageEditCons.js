import { ManageConsForm } from "../components";

const ManageEditCons = () => (
  <section className="page-height overflow-auto">
    <ManageConsForm add={false} />
  </section>
);

export default ManageEditCons;
